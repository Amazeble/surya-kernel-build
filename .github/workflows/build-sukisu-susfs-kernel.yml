name: Build SukiSU-Ultra + SUSFS Kernel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Kernel Source
        uses: actions/checkout@v4
        with:
          repository: Amazeble/android_kernel_xiaomi_surya
          ref: "16.0"
          path: kernel-source

      - name: Inject SukiSU-Ultra and SUSFS
        run: |
          cd kernel-source
          
          # Apply official SukiSU-Ultra SUSFS patch
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s susfs-main
          
          # Ensure SUSFS and KPM are enabled in the defconfig
          echo "CONFIG_KSU=y" >> arch/arm64/configs/surya_defconfig
          echo "CONFIG_KSU_SUSFS=y" >> arch/arm64/configs/surya_defconfig
          echo "CONFIG_KPM=y" >> arch/arm64/configs/surya_defconfig

      - name: Fix Action Bug & Provide GCC Fallback
        run: |
          # The action crashes if these folders don't exist, so we provide a proven 4.14 GCC toolchain
          echo "Bypassing hardcoded gcc-64 check..."
          git clone --depth=1 https://github.com/arter97/arm64-gcc.git /home/runner/gcc-64
          git clone --depth=1 https://github.com/arter97/arm-gcc.git /home/runner/gcc-32

      - name: Build Kernel with AnyKernel3
        uses: dabao1955/kernel_build_action@main
        with:
          kernel-url: ./kernel-source/
          config: surya_defconfig
          arch: arm64
          android-version: "14"
          
          # We handle toolchains manually now to avoid action bugs
          aosp-clang: false
          aosp-gcc: false
          
          # Primary compiler is Proton-Clang
          other-clang-url: https://github.com/kdrag0n/proton-clang
          other-clang-branch: master
          
          anykernel3: true
